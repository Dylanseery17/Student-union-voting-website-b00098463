<form method="post" action="{{ path('proposed_poll_delete', {'id': proposed_poll.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proposed_poll.id) }}">
    <button class="btn">Delete</button>
</form>
